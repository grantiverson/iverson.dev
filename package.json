{
    "name": "iverson.dev",
    "private": true,
    "scripts": {
        "build": "lerna run build",
        "format": "lerna run format",
        "lint": "lerna run lint",
        "prepare": "husky install",
        "start": "lerna run start",
        "test": "lerna run test",
        "verify": "npm install && npm run lint && npm test && npm build"
    },
    "devDependencies": {
        "@babel/preset-env": "^7.20.2",
        "@babel/preset-react": "^7.18.6",
        "husky": "^8.0.3",
        "lerna": "^5.6.2"
    },
    "workspaces": [
        "packages/*"
    ],
    "packageManager": "yarn@3.5.0"
}
